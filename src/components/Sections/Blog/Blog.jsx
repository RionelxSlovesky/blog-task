import SectionTitle from "../../SectionTitle/SectionTitle";


const Blog = () => {
    return (
        <div>
            <SectionTitle title='Blog'></SectionTitle>
        </div>
    );
};

export default Blog;